# Environnement de DÉVELOPPEMENT (versionné — ne contient aucun secret).
# Chargé automatiquement par `next dev`.
# La source de données est la fausse base locale (fixtures) : aucune clé Firebase
# n'est nécessaire pour développer.

NEXT_PUBLIC_APP_ENV=development
NEXT_PUBLIC_DATA_SOURCE=mock
