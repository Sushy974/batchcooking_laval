# Journal de travaux — session autonome
**Date :** 2026-06-16 ~11h15
**Contexte :** le développeur (Sushy974 / Nathan) est absent quelques heures et m'a demandé d'avancer un maximum sur la trame, en mode auto. Tout est fait **sans Firebase** (dev/mock uniquement), validé (typecheck/lint/build + smoke tests runtime) et **poussé sur la branche `dev`**.

Voir le handoff initial : [`2026-06-16_02h03.md`](2026-06-16_02h03.md).

---

## Ce qui a été fait (et poussé sur `dev`)

### Commit `8d55555` — feat(vitrine): pages éditoriales + SEO
- **`/faq`** (type_page `faq`) : accordéon natif `<details>` (pas de JS).
- **`/contact`** (type_page `contact`) : liens tel/email/Instagram selon les toggles + valeurs depuis `config`.
- **`/mentions-legales`** (type_page `mentions_legales`) : rendu des `blocs[]`.
- **`sitemap.xml`** dynamique (routes statiques + plats + formules ; dégrade en cas d'erreur source).
- **`robots.txt`**.
- **Page 404 personnalisée** (`not-found.tsx`).

### Commit `376982c` — feat(admin): tableau de bord en lecture seule
- **core étendu** : `PageRepository.getAll()` (mère + Fake + Firestore) + `GetPagesUseCase` + câblage conteneur (`useCases.getPages`).
- **App admin** (port 3001) :
  - Layout avec **barre latérale** (`AdminNav`) + `UseCasesProvider`.
  - `/` tableau de bord (compteurs plats/formules/pages + encart « lecture seule »).
  - `/plats`, `/formules`, `/pages` : tables de listing.
  - `/config` : vue des paramètres.
  - `apps/admin/src/lib/queries.ts` (lectures « tout inclus », via use cases) + `format.ts`.
  - Thème admin aligné (slate + vert).

### Commit (en cours) — SEO + doc
- **JSON-LD `LocalBusiness`** injecté dans le layout vitrine (Schema.org, important pour le SEO local).
- **README racine** : structure monorepo, archi, environnements, démarrage.
- Ce journal.

---

## État actuel

**Vitrine = fonctionnellement complète** (toutes les routes du cahier) :
`/`, `/plats`, `/plats/[slug]`, `/formules`, `/formules/[slug]`, `/comment-ca-marche`, `/a-propos`, `/faq`, `/contact`, `/mentions-legales` + `sitemap.xml` + `robots.txt` + 404. Plus aucun 404 dans la navigation.

**Admin = squelette lecture seule** (dashboard + listes + config). Pas d'auth, pas d'écriture.

Branche `dev` poussée sur `origin` (Sushy974/batchcooking_laval). `main` non touchée (reste sur l'ancien commit lourd local, non poussé).

---

## Ce qui RESTE (nécessite une décision ou Firebase — donc pas fait en autonomie)

1. **Authentification admin** (Firebase Auth, compte unique Emma) + protection des routes admin.
2. **CRUD admin (écritures)** : étendre les repositories (mère + Fake + Firestore) avec `create/update/delete`, ajouter les use cases correspondants, puis les formulaires d'édition (plats, formules, pages, config). ⚠️ Surface UX importante → vaut mieux cadrer avec le dev avant de coder.
3. **Brancher Firebase** : projet, `.env.*.local`, UID dans `firestore.rules`, déploiement des règles.
4. **Images** : `images.remotePatterns` dans les `next.config.ts` quand de vraies URLs Storage arriveront.
5. **ISR** : passer les pages vitrine de `force-dynamic` à `revalidate` une fois Firestore branché.
6. **Upload d'images** dans l'admin (Firebase Storage).
7. (Optionnel) filtres `/plats` synchronisés dans l'URL.

**Reprise conseillée :** valider avec le dev la direction de l'admin (auth + CRUD), OU brancher Firebase en staging d'abord. Le reste de la vitrine est prêt.

---

## Notes / pièges (rappel)
- Layering strict : UI → use cases (`lib/queries.ts`), jamais Firestore/repos directement.
- `firebase.ts` est lazy — ne pas réintroduire d'init au niveau module.
- `lucide-react` v1 : pas d'icônes de marque.
- Tests runtime : `grep` peut donner des faux négatifs (React découpe `{var} texte` en nœuds).
- `gh` : le compte actif a été basculé sur **Sushy974** pour pouvoir pousser. Pour revenir : `gh auth switch --user AlexandreMaillot`.
